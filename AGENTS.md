# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (MIT LICENSE only, no application code).

**Preferred stack** (from user profile): Next.js App Router, React, TypeScript, Supabase, Vercel, Tailwind CSS, shadcn/ui, Zustand, pnpm.

**Environment**: Node.js v22 and pnpm v10 are pre-installed via nvm. No `.nvmrc` or `.node-version` file exists yet.

**When code is added**: Future agents should re-run environment setup once a `package.json` or other dependency manifest appears. Until then, no update script, lint, build, or test commands are applicable.
